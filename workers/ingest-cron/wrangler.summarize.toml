name = "enggist-summarize-cron"
main = "src/summarize.ts"
compatibility_date = "2024-11-10"

[triggers]
# Run once daily at 2 AM UTC (after ingest has run at midnight)
# This gives time for new posts to be ingested before summarization
crons = ["0 2 * * *"]
